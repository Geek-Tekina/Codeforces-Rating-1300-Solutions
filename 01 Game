#include<bits/stdc++.h>
using namespace std;

void solve()
{
    string s ;
    cin>>s;
    int o =0 , z=0;
    for(auto it : s)
    {
        if(it=='0')
        o++;
        else
        z++;
    }
    if(min(o,z)%2==1)
    cout<<"DA\n";
    else
    cout<<"NET\n";
}
int main()
{
    int t ;
    cin>>t;
    while(t--)
    solve();
}
